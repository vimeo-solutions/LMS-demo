{
  "name": "vimeo-lms-demo",
  "version": "1.0.0",
  "description": "Simulated LMS (Meridian Learning) that consumes a SCORM 1.2 package exported from Vimeo.",
  "main": "server.js",
  "scripts": {
    "build:samples": "node scripts/build-samples.js",
    "prestart": "npm run build:samples",
    "start": "node server.js",
    "predev": "npm run build:samples",
    "dev": "node --watch server.js",
    "lint": "echo \"No linter configured yet\"",
    "test": "echo \"No tests configured yet\""
  },
  "dependencies": {
    "@netlify/blobs": "^11.0.1",
    "adm-zip": "^0.5.17",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-session": "^1.19.0",
    "helmet": "^7.1.0",
    "memorystore": "^1.6.8",
    "multer": "^2.1.1",
    "serverless-http": "^4.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
